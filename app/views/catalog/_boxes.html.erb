<div class="container frontpage-content">
  <div class="row">
    <% ['authors', 'titles', 'periods'].each do |subject| %>
        <%= render :partial => 'box', locals: {document: subject} %>
    <% end %>
  </div>
</div>